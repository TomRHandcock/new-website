# TomRH's Website

This repository contains my portfolio and blogging website using the Hugo framework.

## Prerequisites

* Hugo

## Getting started

1. Clone this repository!
2. Install Git submodules with `git submodule update --init --recursive`.
3. Run the development server with `hugo server`.